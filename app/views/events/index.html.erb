<div class='row'>
  <div class='col-sm-2 left-gutter'>
  </div>
  <div class='col-sm-8 center'>
    <% @events.each do |event| %>
      <%= render partial: "event_display_for_index", locals: {event: event} %>
    <% end %>
  </div>
  <div class='col-sm-2 right-gutter'>
  </div>
</div>